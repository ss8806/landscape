import React, { SyntheticEvent, useEffect } from "react";

type Props = {
    categories: any;
};

// const cate = categories.map((category: any, index: number) => {
//   return (
//       <option key={category.id} value={category.id}>
//           {category.name}
//       </option>
//   );
// });

const Category: React.FC<Props> = ({}) => {
    return <></>;
};

export default Category;
